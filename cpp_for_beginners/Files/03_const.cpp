#include <iostream>

int main()
{
    /*
        It's better if we name constants in the uppercase,
        as it helps differentiate them from other variables
        defined in the program
    */
    const double PI = 3.14159;
    // PI = 42069;
    /*
        this will show an error if we try to change the const
        value like any variable.
    */


   return 0;
}