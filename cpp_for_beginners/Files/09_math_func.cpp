#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    double x = 3;
    double y = 4;
    double z;

    // z = max(x,y);
    // z = min(x,y);

    // z = pow(2,4);
    //pow(base, exponent)

    // z = sqrt(9);
    // z = abs(-3);


    // z = round(3.14);
    // it will round and print 3.

    // z = ceil(4.16);
    // it will make 4.16 -> 5.

    // z = floor(4.23);
    // it will make 4.23 -> 4

    cout << z;

    return 0;
}
